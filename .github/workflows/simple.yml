name: Simple Pipeline

on: [push]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Smart Campus Energy Optimizer"
      - run: echo "Pipeline is working!"